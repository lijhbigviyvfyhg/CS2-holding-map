import { Instance } from "cs_script/point_script";

// Workshop builds now keep builtin route data inside autopeek_training.js.
// This point_script remains as a harmless compatibility stub for older map entities.
Instance.OnActivate(() => {});
